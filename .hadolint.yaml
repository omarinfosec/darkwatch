ignored:
  # DL3008: pin apt-get versions — too noisy, addressed via base image pinning instead
  - DL3008
  # DL3013: pin pip versions — addressed in requirements.txt with ==
  - DL3013

trustedRegistries:
  - docker.io
  - ghcr.io
  - quay.io

failure-threshold: warning
